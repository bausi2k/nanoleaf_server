# docker-compose.yml (unverändert, Port-Mapping ist implizit 8667:8667 durch host mode)
version: '3.8'

services:
  nanoleaf-controller:
    build: .
    container_name: nanoleaf-controller
    network_mode: host # WICHTIG: Erlaubt mDNS
    env_file:
      - .env
    restart: unless-stopped